# Núcleo
pandas
matplotlib

# LLM (opcional): Gemini se llama por REST (urllib, sin dependencia).
# Anthropic solo si se usa ese proveedor:
anthropic
# Hace que el SDK valide TLS contra el almacén de certificados del sistema.
# Necesario en Windows con antivirus/proxy que intercepta TLS: sin esto el SDK
# muere con CERTIFICATE_VERIFY_FAILED (certifi no trae esa raíz). Ver README.
truststore

# Tests
pytest

# Notebooks / experimentos (opcional)
nbformat
nbconvert
ipykernel

# El detector clásico "de verdad" de Trust usa estas (en este entorno los
# modelos no se pudieron descargar por SSL; ver docs/metodologia.md):
# spacy
# stanza
